let mass = []
let n = 40
let m = -5
let count = 42
let i = 0
// while(mass.length < count){
//     num = Math.round(Math.random()*Math.abs(m-n) + Math.min(n,m))
//     mass.push(num)
// }
// do{
//     num = Math.round(Math.random()*Math.abs(m-n) + Math.min(n,m))
//     mass.push(num)
// }while(mass.length < count)
// for(i=0; i < count;i++){
//     num = Math.round(Math.random()*Math.abs(m-n) + Math.min(n,m))
//     mass.push(num)  
// }
console.log(mass)
